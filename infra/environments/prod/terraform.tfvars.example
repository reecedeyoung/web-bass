project     = "k-strong-bass"
environment = "prod"
aws_region  = "us-east-1"
domain_name = "web-bass.com"

callback_urls = [
  "https://web-bass.com",
  "https://www.web-bass.com",
]

logout_urls = [
  "https://web-bass.com",
  "https://www.web-bass.com",
]

github_subject_claim = "repo:YOUR_ORG/YOUR_REPO:environment:prod"

# Cloudflare dashboard → web-bass.com → Overview → Zone ID (right sidebar)
cloudflare_zone_id = "YOUR_CLOUDFLARE_ZONE_ID"

# dash.cloudflare.com → My Profile → API Tokens (needs Zone:DNS:Edit)
cloudflare_api_token = "YOUR_CLOUDFLARE_API_TOKEN"
